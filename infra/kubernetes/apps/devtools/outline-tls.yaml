apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: docs-gramly-tech
  namespace: traefik-public
spec:
  secretName: docs-gramly-tech-tls
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - docs.gramly.tech
---
apiVersion: gateway.networking.k8s.io/v1
kind: ReferenceGrant
metadata:
  name: collaboration-docs-tls
  namespace: traefik-public
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: Gateway
      namespace: traefik-collaboration
  to:
    - group: ""
      kind: Secret
      name: docs-gramly-tech-tls
